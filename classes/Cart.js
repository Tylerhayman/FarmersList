class Cart {
    constructor(products,total) {
        this.products = products = [];
        this.total = 0;
    }


    addProduct() {
        this.products.push(Product);
        Product + this.total;
    }


    removeProduct(i) {
        this.products.pop(Product[i]);
        this.total - Product[i];
    }
}

module.exports = Cart